This directory is mounted as /usr/local/retool-git-repo in the `api` and
`test-runner` service containers.

Learn more about local development and Git Syncing
[here](https://docs.retool.com/docs/git-syncing).
